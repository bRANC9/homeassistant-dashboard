version: "3.8"

services:
  webhook:
    image: almir/webhook
    container_name: ha-dashboard-webhook
    restart: unless-stopped
    ports:
      - "9000:9000"
    volumes:
      - ./hooks.json:/etc/webhook/hooks.json
      - ./deploy.sh:/deploy/deploy.sh:ro
      - ./validate.sh:/deploy/validate.sh:ro
      # HA config könyvtár — TrueNAS dataset mount
      # Pl. /mnt/pool/homeassistant/config
      - ${HA_CONFIG_PATH}:/ha-config
      # Deployment checkout
      - deploy-data:/deploy/repo
    environment:
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - HA_CONFIG_PATH=/ha-config
      - HA_TOKEN=${HA_TOKEN}
      - HA_URL=${HA_URL:-http://localhost:8123}
    command: "-hooks /etc/webhook/hooks.json -verbose"

volumes:
  deploy-data:
    driver: local
